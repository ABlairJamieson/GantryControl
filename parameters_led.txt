Nscan     = 200                    # Number of scan points (approx)
Rscan     = 450                    # Radius from camera to scan (mm)
ledpos    = 800, 500, -600         # Camera position (mm, mm, mm)
ledfacing = 0.0, -1.0, 0.0         # Camera facing nx, ny, nz (normalized)
phimin    = -80                    # Degrees min from cam coord (x-axis right looking at cam)
phimax    = 80                     # Degrees max from cam coord
thetamin  = 20                     # Degrees min from z-axis (relative upward)
thetamax  = 85                     # Degrees max from z-axis 
interval  = 10			   # data collection time at each point (seconds)
